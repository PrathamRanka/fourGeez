# RL notebooks

Notebooks may be used for exploration and visual analysis only.

Any behavior needed for training, inference, evaluation, or reproducibility must be moved into `src/agentpay_rl` and covered by tests. Clear outputs before committing notebooks, and never embed credentials or production data.

