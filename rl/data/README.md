# RL data directory

Store schema examples and generated synthetic datasets here during development.

- `fixtures/` may contain small reviewed synthetic fixtures committed to Git.
- `generated/` must remain untracked.
- Never commit production transaction data, prompts, wallet details, approval tokens, payment proofs, or personal information.
- Every dataset must record schema version, generator version, seed, creation time, and synthetic/real classification.

